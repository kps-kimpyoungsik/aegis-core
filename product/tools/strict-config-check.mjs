import fs from "node:fs";const config=JSON.parse(fs.readFileSync(new URL("../tsconfig.json",import.meta.url),"utf8"));const c=config.compilerOptions??{};const required=["strict","noUncheckedIndexedAccess","exactOptionalPropertyTypes","noImplicitOverride","useUnknownInCatchVariables","forceConsistentCasingInFileNames","noEmit"];for(const key of required){if(c[key]!==true)throw new Error(`AEGIS-TS-001 STRICT_FLAG_REQUIRED ${key}`);}console.log(`strict-config-check PASS (${required.length} strict flags)`);
